# This file exists so pytest treats the project root as the base
# for imports, allowing `import pcb_vision_pipeline` to work from
# inside tests/test_pcb_vision_pipeline.py regardless of where
# pytest is invoked from.
